--ORDER TO RUN

Reco
-CastorTreeProducer2 -> creates tree

Histograms
-csana.cc -> creates set of histos
-noiseana.cc -> creates set of histos for noise plots

HF energy calibration
-required for all plots
-Thus, makePlots_perf_ring_corr_factors.C turn on variable in csana.cc and rerun.
-It is a good idea to rename the previos histos.root output to sth like ..._nohfcalibration

Plotting
-makePlots_cs_eff.C -> generates MC efficiencies in root file
-makePlots_cs.C -> generates CS values in root file
-makePlots_cs_dependence.C -> checks dependence on threshold
-makePlots_concl* -> final histograms
-makePlots*.C --> all the other plots
